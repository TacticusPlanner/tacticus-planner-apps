// Exposed to entities/account: the V1 import result carries parsed goal specs
// that the client re-submits via entities/goal's createCombinedGoals mutation.
export type { CreateCombinedGoalsRequest } from "../model/types"
