## 1. Client Catalog Contract

- [x] 1.1 Widen `kind` from a closed `meta`/`alternate` enum to a non-empty string, add `efficiency`, `primeUnitSetIds`, and a `primes[]` array (same recommendation shape as `bosses[]`) to the client `guild-raid-meta` schema, storage, and query surface, and verify schema tests cover a valid multi-recommendation boss, a non-positive `efficiency`, a duplicate `kind` within one group, and a `primes[]` entry.
- [x] 1.2 Add a query for a prime's curated recommendations by `unitSetId`, mirroring the existing boss lookup, and extend `queries.guild-raid-meta.test.ts` to assert `efficiency`/`primeUnitSetIds`/`primes[]`/multi-recommendation boss groups survive sync/replace with authored ordering preserved. Added `getGuildRaidMetaForPrime`.

## 2. Investment-Readiness Calculation

- [x] 2.1 Add a pure function deriving the boss-wide required `Progression`/rank/ability threshold from `raid-bosses` `StatProgression` and the live `GuildRaidBossStatus.progressionIndex`, and verify tests cover a mid-ladder step, the first step, and the last step (including the last step's above-character-cap `AbilityLevel`/`Rank` values, which this derivation never reads directly). Added `resolve-guild-raid-investment-threshold.ts`.
- [x] 2.2 Add the three-ratio hero-readiness function (rank/progression/ability, each capped at 100%, averaged) and the single-ratio Machine-of-War readiness function, and verify tests cover unowned (0%), fully-sufficient (100%), and each single-dimension shortfall in isolation. Added `resolve-guild-raid-investment-readiness.ts`.
- [x] 2.3 Extend `use-guild-raid-exact-readiness.ts`'s investment maps to carry `progressionIndex`, `activeAbilityLevel`, and `passiveAbilityLevel` alongside the existing `xpLevel`/`rank`, and verify a test confirms the additional fields flow from the roster query already fetching them. Extracted the mapping into a pure `buildGuildRaidRosterInvestment` (in `build-guild-raid-exact-readiness-view.ts`) so it's directly unit-testable.
- [x] 2.4 Add the weighted team-readiness combinator (essential slots weighted higher than flex/Machine-of-War) and verify a test proves an essential-slot shortfall lowers team readiness more than an equal flex-slot shortfall. Added `resolve-guild-raid-team-readiness.ts`.
- [x] 2.5 Add the live-status-absent fallback to ownership-only Ready/Partial/Unavailable, and verify a test covers a missing/non-`active` `GuildRaidBossStatus`. Added `resolveGuildRaidLiveInvestmentThreshold` (takes plain `isObservationActive`/`liveProgressionIndex` values rather than importing the `guild-raid-status` entity's types, keeping `entities/guild-raid-meta` independent of it — a page/feature composes the two).

## 3. Candidate Matching and Flex-Slot Display

- [x] 3.1 Port the constrained-assignment search (lock owned ideal heroes; search missing slots' `replacementCharacterIds`; maximize filled essential slots, then total filled slots) into `entities/guild-raid-meta`, ranking tied candidates by investment-readiness percentage instead of combat power, and verify table-driven tests cover locked heroes, duplicate-candidate conflicts, and impossible-completion essential-first reservation (adapted from the removed `add-dailies-guild-raid-playable-variants` test plan). Added `match-guild-raid-candidates.ts` (exhaustive backtracking search, not greedy, over the small open-slot solution space).
- [x] 3.2 Resolve and expose every owned candidate's readiness percentage for a slot whose ideal hero is unowned or under-invested (not only the matcher's selected candidate), and verify a test covers two owned replacements on the same slot. Added `resolve-guild-raid-slot-candidates.ts`.
- [x] 3.3 Preserve authored recommendation ordering through matching and scoring, and verify a test proves team readiness, candidate percentages, and matching never reorder recommendations. Added `resolve-guild-raid-recommendation-readiness.ts` orchestrator.

## 4. Presentation and Responsive UI

- [x] 4.1 Resolve `kind` through the same known-id/readable-fallback pattern already used for `roleId`, and verify a resolver test covers a known archetype id and an unknown one. Added `resolveKind`/`GuildRaidMetaKindPresentation` to the entity's presentation resolver (mirrors `resolveRole`), wired a `kindLabel` translation callback in `use-guild-raid-meta-presentation.ts`, and updated the two existing raw `t(\`...kind.${kind}\`)`/`t(\`raidBosses.meta.${kind}\`)`call sites (Library's`guild-raid-views.tsx`, Dailies' `guild-raid-exact-meta-card.tsx`) that the widened free-form `kind` type broke at compile time.
- [x] 4.2 Extend recommendation cards to show per-hero/Machine-of-War readiness percentages, the combined team percentage, and every owned flex-slot candidate's percentage, and verify component tests cover a fully-ready, partially-ready, and unready recommendation, plus a boss with three recommendations rendering all three cards. Added the page-local `useGuildRaidInvestmentReadiness` hook (composes catalog + `raid-bosses` + roster, independent of `entities/guild-raid-status`), wired it through `GuildRaidExactMetaRegion`/`GuildRaidsPage`, extended `GuildRaidExactMetaCard` with an optional `readiness` prop (falls back to the unchanged ownership-only display when absent), and added `id`/`progressionIndex` fields the wiring needed (`GuildRaidExactReadinessRecommendationView.id`, `GuildRaidBossView.progressionIndex`).
- [x] 4.3 Add read-only curated-comp cards for a prime's `primes[].recommendations` (roster, roles, Machine of War, `efficiency`, resolved `kind` label — no readiness percentage or matcher), reusing `resolveRecommendation`, and verify a component test covers a prime with a curated comp and one without (falling back to the existing roster-agnostic display). Added `guild-raid-prime-comp-card.tsx`, wired below each `GuildRaidPrimeCard` in `guild-raid-status-region.tsx`; renders nothing (the existing roster-agnostic `GuildRaidPrimeCard` alone) when the catalog has no curated comp for that prime.
- [x] 4.4 Extend the desktop (≥768px) and mobile (<768px) layouts per the modified spec's ordering, and verify viewport tests assert no horizontal scrolling below 768px. `GuildRaidExactMetaCard` already put team-readiness/missing-count before the collapsible lineup on mobile and kept everything visible together on desktop (from 4.2); added tests asserting the summary precedes the lineup in DOM order and that heroes/candidates rows carry `flex-wrap` (no fixed-width overflow).
- [x] 4.5 Add the live-status-unavailable fallback state to the page, and verify a page test confirms ownership-only display with no fabricated percentage. The fallback was already structural (card renders plain ownership display when `readiness` is `undefined`, from 4.2/2.5); added an explicit page-level test confirming no team-readiness or per-hero-readiness badge renders when `useGuildRaidInvestmentReadiness` reports `"unavailable"`.

## 5. Copy, Tutorial, and Live Verification

- [x] 5.1 Add all readiness-percentage, candidate-comparison, prime-comp-card, and known-`kind` label copy to every supported locale with real de/es/fr translations, and verify locale key parity tests pass. Added `teamReadiness`/`candidatesLabel`/`efficiencyLabel` and widened `kind` to all 7 archetype ids the sourced data actually uses (`meta`/`alternate`/`lavistodes`/`neuro`/`battlesuit`/`admech`/`custodes`) across en/de/es/fr; the prime-comp-card reuses these same keys (no new copy of its own). `dailies-translations.test.ts` (the locale key parity test) passes for all three non-English locales.
- [x] 5.2 Update `guild-raids.tutorial.tsx` and translated `tour.guildRaids.steps.*` keys for the percentage display, and verify tutorial tests resolve the breakpoint-specific targets. Step targets/structure were already correct (still point at `guild-raid-exact-meta-section`, which now contains the percentage UI); updated the `exactMetaDesktop`/`exactMetaMobile` step copy in all four locales to mention readiness percentage/team score instead of only ownership. `guild-raids-page.test.tsx`'s desktop/mobile target-resolution tests pass unchanged.
- [ ] 5.3 Use the full Aspire stack and authenticated browser to verify, below and at/above 768px: a fully-ready hero, a partially-ready hero with visible ratio breakdown, an unowned hero, multiple flex-slot candidates with distinct percentages, essential-vs-flex weighting in the team score, the live-status-unavailable fallback, and both tutorial variants; record screenshots/notes in the PR. **Partially verified live**, against the running Aspire stack with the user's own real, active-season guild (Ghazghkull boss) — confirmed at ≥768px: fully-ready heroes (100% badges), partially-ready heroes (e.g. 46%/60%/84%/92%), multiple flex-slot candidates with distinct percentages ("Alternatives: admecDominus - 81%, worldExecutions - 100%"), the prime-comp cards, and zero console errors. **Not verified live** (this guild's real data doesn't currently present these states, and the browser-automation resize tool did not affect the user's actual window, which stayed at 2560px throughout): the <768px layout, an unowned/0%-readiness hero, the live-status-unavailable fallback, essential-vs-flex weighting isolated from a real number, and the tutorial walkthrough itself (steps were confirmed to target real elements via `guild-raids-page.test.tsx`, but not walked through live). These remaining states are covered by the automated component/page tests added in this change (4.2, 4.4, 4.5) but not by a live pass — leaving this unchecked rather than claiming full live coverage.

## 6. Cleanup

- [x] 6.1 Remove the `add-dailies-guild-raid-playable-variants` change directory (proposal, design, tasks, spec delta) — none of its tasks were implemented and this change supersedes it. Removed earlier this session (`git rm -r`); confirmed gone.

## 7. Companion and Repository Gates

- [x] 7.1 Against the applied companion API change, synchronize `guild-raid-meta` and verify the downloaded record's `efficiency`/`primeUnitSetIds`/`primes[]`/widened-`kind`/sourced rosters pass validation with exact ordering intact. Verified live against the running Aspire stack (real signed-in session, real guild data): IndexedDB's `guild-raid-meta` row has 12 bosses/15 primes, `sourceId: "terminus-maximus-and-cognitae-guild-raid-meta"`, widened `kind` values present alongside the original `meta`/`alternate` (e.g. Ghazghkull's boss group: `battlesuit`/`lavistodes`), `efficiency` present and ordered strongest-first per group, `primeUnitSetIds` populated, and a `primes[]` entry's recommendation carries `id`/`kind`/`efficiency`/5 `heroSlots`/`mowId` intact.
- [x] 7.2 Run `pnpm test:run` and verify all workspace tests pass. 1326/1326 tests pass across 198 files.
- [x] 7.3 Run `pnpm typecheck` and verify it exits successfully. Passes clean.
- [x] 7.4 Run `pnpm lint` and `pnpm lint:fsd` and verify both exit successfully. Both clean (fixed one unused-eslint-disable warning along the way); `lint:fsd` (steiger) reports no FSD boundary problems, confirming `entities/guild-raid-meta` stays independent of `entities/guild-raid-status`.
- [x] 7.5 Run `git diff --check` and verify no whitespace errors are reported. Exit 0 — only benign LF/CRLF autocrlf notices, no whitespace errors.
