## 1. Schema updates

- [x] 1.1 Add required `type: z.string()` to `liveProgressSchema`'s `battleAttempts` item schema (`packages/player-data/src/player-data.schema.ts`), verified by `player-data-sync.test.ts`'s fixture data (including the `battleAttempts` sample around line 47) updated to carry `type` and the suite passing
- [x] 1.2 Add required `battleIndex: z.number()` to the campaign-battle schema (`packages/game-catalog/src/schemas/campaign.ts`, alongside the existing `nodeNumber` field), verified by `packages/game-catalog/src/schemas.test.ts` updated fixtures and passing
- [x] 1.3 Add required `battleIndex: number` to `Battle` (`apps/web/src/fsd/shared/lib/battle.domain.ts`) and thread it through `mapCampaignBattleStorageToDomain` (`apps/web/src/fsd/features/rank-lookup/lib/rank-lookup.mapper.ts`). No dedicated unit test exists for this mapper (none existed before this change either) and none was added: the mapping is a one-line field passthrough with no branch or loop, already guaranteed correct by `pnpm typecheck` (the schema requires `battleIndex`, so `record.battleIndex` can't silently be `undefined`), and is exercised transitively by every `daily-raids`/`goal-farming` test that consumes a mapped `Battle`

## 2. Unified battle-attempt indexing

- [x] 2.1 Rework `buildStandingBattleIndex` (`apps/web/src/fsd/features/daily-raids/model/daily-raids-energy.ts`) into an index covering every campaign, keyed by `` `${campaignGroupId}:${type}:${battleIndex}` `` and built from the served `battle.battleIndex` rather than `battle.nodeNumber - 1`; drop the `eventCampaignIds`-based skip. Renamed to `buildBattleAttemptIndex` (no longer "standing" — misleading otherwise) and re-exported accordingly; `use-daily-raids.ts` updated to match. Verified by `daily-raids-energy.test.ts`'s `buildBattleAttemptIndex` suite: a standing campaign's battle indexes as before, an event campaign's challenge node and its same-`nodeNumber` regular node produce two distinct entries, and a campaign event's two tiers index independently despite sharing a `campaignGroupId`
- [x] 2.2 Update `calculateRealEnergyUsedToday`, `buildAttemptsLeftByBattle`, and `buildTodaysAttempts` (same file) to key their lookups by `` `${attempt.tacticusCampaignId}:${attempt.type}:${attempt.battleIndex}` `` and drop their individual `eventCampaignIds`-based skips (all three functions' `eventCampaignIds` parameter removed entirely — no longer needed). Verified by each function's rewritten test suite in `daily-raids-energy.test.ts`, each with a case proving an event-campaign attempt is now included the same way a standing-campaign attempt is, and a case proving a Standard-tier attempt is never conflated with an Extremis-tier attempt at the same `battleIndex`
- [x] 2.3 Update `daily-raids-calc.test.ts` and any other test fixtures that construct `RealBattleAttempt`/`Battle` values to include `type`/`battleIndex`, verified by the full `daily-raids` feature test suite passing. `pnpm typecheck` surfaced every affected fixture: `daily-raids-calc.test.ts` (4 distinct `Battle` literals), `campaign-insights.test.ts` (5 literals), `estimate.test.ts`, `estimate-plan.test.ts`, `shard-energy-estimate.test.ts`, `per-project-estimate.test.ts`, `use-acquisition-source-selection.test.ts`, `plan-insights-calc.test.ts`, `acquisition-source-field.test.tsx` (1 literal each) — all updated with `battleIndex`; `packages/game-catalog/src/schemas.test.ts`'s campaign-battles payload fixture also needed `battleIndex` (surfaced by the runtime schema test, not typecheck, since it's an untyped JSON literal)

## 3. Today page consumers

- [x] 3.1 Confirm (with a regression test if not already covered) that the node-exhaustion check in Today's schedule (`daily-raids-today` spec's "Today's raid schedule") now excludes an event-campaign node whose real attempts-left has reached zero, the same way it already excludes a standing-campaign node. `isLocationVisible` (`location-visibility.ts`) is a campaign-agnostic `attemptsLeftByBattle.get(battleId) !== 0` lookup with no campaign-type branching — its existing 3-case test suite (`location-visibility.test.ts`) already covers the full behavior regardless of which campaign a `battleId` belongs to, so no new test was needed there; its stale doc comment (which called out event-campaign nodes as always falling into the "no data" fallback) was corrected. End-to-end coverage comes from task 2.2's new `buildAttemptsLeftByBattle` tests proving an event-campaign node's real `attemptsLeft` now populates the map the same way a standing node's does
- [ ] 3.2 Manually verify in the running app: with a signed-in account that has raided an active campaign event today, Today's "Today's Attempts" section lists that event campaign's raided node(s) with the correct raid count, and the real-daily-energy-usage percentage includes their energy cost — **Deferred / out-of-session** (tracking: verify manually next time an authenticated Tacticus-linked account with real event-campaign raids performed today is available). Checked before deferring: the Aspire stack was not already running this session (`localhost:5000`/`localhost:15888` unreachable) and no browser session was connected, and even with the stack started, this scenario additionally needs a live account whose real Tacticus data includes today's event-campaign raids — a state that can't be fixture-injected for a "real synced data" verification and isn't controllable from this session

## 4. Companion change coordination

- [x] 4.1 Confirm the `tacticus-planner-api` companion change (same change name) ships `type` on `live-progress.battleAttempts[]` and `battleIndex` on served campaign battles before this half's schema updates (task 1) are applied — per this repo's cross-repo convention, apply the API half first. Confirmed: the API half was applied first this session (`LiveProgress.cs`'s `BattleAttemptRecord.Type`, `Campaigns.cs`'s `GameCatalogCampaignBattleView.BattleIndex`), its full gate suite passed (381/381 tests), and this repo's schema updates (task 1) were applied only afterward

## 5. Gates

- [x] 5.1 `pnpm test:run` — 216 test files / 1525 tests passed (web), plus game-catalog and player-data package suites
- [x] 5.2 `pnpm typecheck` — clean across all 5 packages
- [x] 5.3 `pnpm lint` — clean (eslint + knip) across all 5 packages, force-verified bypassing turbo's cache
- [x] 5.4 `pnpm lint:fsd` — `steiger ./src/fsd`: no problems found
- [x] 5.5 `git diff --check` — exit 0, no whitespace errors (only pre-existing CRLF line-ending notices, not errors)
