## 1. XP-book allocation primitives

- [x] 1.1 Refactor `netXpAgainstOwnedBooks` in `level-xp-cost.ts` into a `consumeOwnedBooks` primitive that also returns the updated book pool (spent books removed), with `netXpAgainstOwnedBooks` becoming a thin wrapper around it (`.remainingXp`) so its existing callers/tests are unaffected; verify with unit tests that a second goal's allocation sees what a first goal already spent, and that the input pool isn't mutated
  - Done; `consumeOwnedBooks` added, `netXpAgainstOwnedBooks` unchanged in behavior. 3 new tests in `level-xp-cost.test.ts`.
- [x] 1.2 Add `maxLevelReachableWithXp(currentLevel, currentXp, targetLevel, availableXp)` — the inverse of `xpNeededForLevelRange`, scanning candidate levels since the XP curve has no closed-form inverse (mirrors `reachableRankProgress`'s own candidate-scan pattern for the Rank ceiling marker); verify with unit tests covering zero-available-xp, a partial climb, capping at targetLevel, and crediting already-earned partial xp
  - Done; 5 tests in `level-xp-cost.test.ts`.
- [x] 1.3 Export both from `@/features/goal-farming`'s public API alongside the existing `xpNeededForLevelRange`/`ownedBooksByRarity` (previously internal to the slice)

## 2. Remaining-text and Actual/Potential explanation

- [x] 2.1 Add `remainingXp: number | null` to the `Level` `GoalProgress` variant in `goal-progress.ts`, computed via `xpNeededForLevelRange` from the character's/MoW's true current level and xp; verify with unit tests for a nonzero gap (crediting partial xp already earned) and the already-reached case
  - Done; 2 new tests plus updated existing Level fixtures in `goal-progress.test.ts`.
- [x] 2.2 Update the Level branch of `formatGoalRemainingText` (`goal-remaining-text.ts`) to append the xp figure when `remainingXp` is nonzero (`"{{count}} levels · {{xp}} XP"`), falling back to the existing `"{{count}} levels"` string; add the new i18n key to all 4 locales; verify with unit tests for both branches
  - Done; 2 tests (one new, one clarified) in `goal-remaining-text.test.ts`.
- [x] 2.3 Extend the explanation popover's Actual/Potential clauses (`goal-progress-visuals.tsx`) to cover a Level goal the same way they already cover a Rank goal — Actual line notes remaining levels, Potential line notes remaining xp; add the new i18n keys to all 4 locales; verify with a unit test asserting both clauses render for a Level goal with a potential ratio
  - Done; 1 test in `goal-progress-visuals.test.tsx`.

## 3. Shared xp-book allocation across a project's Level goals

- [x] 3.1 Add `level-potential-allocation.ts` (`buildLevelGoalNeeds`, `allocateXpBooksAcrossGoals`), mirroring `orb-potential-allocation.ts`'s shape: walk goals in priority order, spend from one shared, mutated book pool per goal (all-or-nothing per book, via `consumeOwnedBooks`); verify with unit tests for full coverage, partial coverage, priority ordering (higher-priority goal claims first), and no-books-owned
  - Done; 5 tests in `level-potential-allocation.test.ts`.
- [x] 3.2 Wire `computePlanInsights` (`plan-insights-calc.ts`) to build `LevelGoalNeed`s from `orderedDetails` (a Level goal never produces the `need`/`stages` output the main per-detail loop's body is gated behind, so it needs its own pass — split into `buildLevelGoalNeeds` to keep the file under this repo's max-lines rule), call `allocateXpBooksAcrossGoals`, and set a ratio into `potentialProgressByGoalId` for every Level goal with a computed allocation; verify with unit tests for a single Level goal and for two Level goals sharing a pool by priority
  - Done; 2 tests in `plan-insights-calc.test.ts`.
- [x] 3.3 Fetch the account's XP-book inventory (`getInventoryXpBooks`) in `use-plan-insights.ts` alongside the existing upgrade/orb inventory fetches, include it in the memoized `calculationKey`, and pass it through to `computePlanInsights`
  - Done.

## 4. Cross-cutting verification

- [x] 4.1 Run `pnpm test:run`, `pnpm --filter web typecheck`, `pnpm lint`, `pnpm --filter web lint:fsd`, and `git diff --check`; fix any failures before considering this change done
  - All pass (re-run after 4.2's fix below): 1707/1707 tests, clean typecheck/lint/lint:fsd, `git diff --check` exit 0 aside from CRLF-conversion warnings. (`plan-insights-calc.ts` needed the split in 3.2 to stay under the 400-line max-lines rule; two existing test files' `@workspace/player-data/queries` mocks needed a `getInventoryXpBooks` stub added once `usePlanInsights` started calling it.)
- [x] 4.2 Manually verify against the running local stack, using the account's own synced project ("My Goals") and XP-book inventory: confirm the Remaining column shows "N levels · N,NNN XP" for a Level goal with xp still needed (and plain "N levels" for one already covered by total xp already gained); confirm a Level goal in the project view shows the striped Potential fill and "↗ N%" when the account owns applicable xp books; confirm the info popover's Actual/Potential lines show the levels-remaining/xp-remaining clauses with real figures; confirm a lower-priority Level goal shows no Potential lift once higher-priority Level goals have exhausted the shared book pool
  - **A real bug was found during this pass**: `xpNeededForLevelRange` treated `currentXp` as a per-level-reset partial amount, but the real Tacticus API's `xp` field is the character's _total_ xp gained since level 1 — see design.md Decision 5 for the full diagnosis and the fix (`xpNeeded = targetLevelTotalXp - currentXp`, dropping the double-subtracted `currentLevelTotalXp` term). Found live: two different real Level goals spanning the same 6-level range independently landed on the exact same implausible "5 of 6 levels" (83%) Potential ratio, and two other real Level goals showed no XP figure in the Remaining column at all (the bug's double-subtraction went negative and clamped to 0 for a small level-span relative to the character's real total xp). Cross-checked the fix against V1's own production app (`tacticusplanner.app/plan/goals`) for the same account: its own "Total Resources Missing" aggregate reports ~3.4M XP still needed (≈54 Mythic-book-equivalents, 62,500 xp each) across the whole plan — the same order of magnitude the fix produces per-goal (hundreds of thousands to low millions each), and inconsistent with the pre-fix ~100-200K-per-goal figures. All affected unit tests (`level-xp-cost.test.ts`, `level-potential-allocation.test.ts`, `goal-progress.test.ts`, `plan-insights-calc.test.ts`) recomputed and updated against the corrected xp-curve math.
  - Re-verified live after the fix, against the real synced account and its "My Goals" project: the Remaining column now reads e.g. "6 levels · 1,304,192 XP" for Neurothrope and "2 levels · 46,503 XP" for Arjac (previously showing no XP figure at all — the bug this task caught). At this point every Level goal still showed 0% Potential, attributed (incorrectly, see below) to the account's owned-book stock being a small fraction of these high-level characters' real xp cost.
  - **Superseded — a second real bug was found**: the user reported "why 0 potential? v1 does not show zero" with a V1 screenshot showing non-zero progress for Level-adjacent goals on the same account, directly contradicting the note above. Investigation found `xpBookIdRarity`'s id map used guessed `bookXxx` keys, but the real synced ids (confirmed via read-only inspection of the app's own IndexedDB `inventory:summary` record) use an `xp` prefix (`xpCommon`/`xpLegendary`/etc.) — see design.md Decision 6. This silently zeroed the entire owned-books pool for every Level goal, independent of the fix above. Fixed the map; re-ran the full gate (1707/1707 tests, clean typecheck/lint/lint:fsd/`git diff --check`) and re-verified live: higher-priority Level goals now show real Potential lift (Neurothrope 100%, Ahriman 100%, Abraxas 83%), while lower-priority ones (Arjac, Ragnar) still correctly show 0% once the account's ~3.9M-XP pool is exhausted by higher-priority goals first — expected shared-pool depletion, not a bug.
