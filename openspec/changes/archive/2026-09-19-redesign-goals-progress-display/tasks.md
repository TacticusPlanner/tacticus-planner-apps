## 1. Sequencing check

- [x] 1.1 Run `openspec status --change fix-goal-progress-consistency --json` and confirm it has been archived (its delta merged into `openspec/specs/goal-progress-display/spec.md`) before treating this change's `goal-progress-display` MODIFIED requirements as ready to implement against a real base spec; if not yet archived, pause and ask before continuing (see proposal.md's Impact / design.md's Migration Plan)
  - Archived (with user confirmation) as `openspec/changes/archive/2026-09-19-fix-goal-progress-consistency`; `goal-farming-estimates` and `goal-progress-display` synced into `openspec/specs/`. This change's MODIFIED requirement's scenario coverage was also reconciled against the real base spec (`openspec validate --strict` caught 3 scenarios the delta had renamed rather than preserved) and fixed before continuing.

## 2. Shared stacked progress bar + percent label

- [x] 2.1 Add a `StackedProgressBar` component in `goal-visuals.tsx` (track + striped potential fill + solid actual fill, both ratios clamped to 0–1, potential fill hidden when absent or not greater than actual, 6px desktop / 5px mobile height per `useIsMobile()`, rounded ends), with one accessible `role="progressbar"` element carrying `aria-valuenow`/`aria-valuetext` summarizing both ratios; verify with unit tests covering every edge case in `goal-progress-display`'s "Progress renders as one stacked bar…" requirement (actual-only, potential > actual, potential ≤ actual, actual 0 with potential > 0, 100% complete)
  - Implemented in the new `goal-progress-visuals.tsx` (split out of `goal-visuals.tsx` to keep files under this repo's 400-line lint rule); the "striped" fill uses a `repeating-linear-gradient` against the theme's real `--primary` CSS variable rather than a guessed color.
- [x] 2.2 Add the percent label (rounded tabular-number actual %, plus a smaller "↗ {{potential}}%" line only when potential exceeds actual) next to the bar; verify with unit tests for both branches in "Progress renders a percent readout…"
- [x] 2.3 Delete the old two-`Progress`-bar rendering, the per-row "Actual progress"/"Potential progress" caption spans, and the always-visible explanation `<p>` elements from `GoalProgressDisplay`, replacing them with the new stacked bar + percent label. Keep the `goal-progress-bar` testid on the new `StackedProgressBar`'s single accessible element (it's still "the progress bar," now covering both the actual-only and dual-ratio cases) rather than renaming it — update its `aria-valuetext` format (design.md's a11y risk note) and the two existing assertions on that value in `goals-list.test.tsx:229,247` accordingly. Remove the now-obsolete `goal-potential-progress-bar`/`goal-actual-progress-description`/`goal-potential-progress-description` testids and their assertions in `goal-visuals.test.tsx`, `goal-detail-view.test.tsx`, and `goals-list.test.tsx`; verify with a unit test that they no longer render and that `goal-progress-bar` still does, on both the single- and dual-ratio cases

## 3. Remaining-text formatter

- [x] 3.1 Add a per-goal-kind remaining-text formatter (Level: "{{count}} levels"; Rank: "{{slots}} slots" or "{{slots}} slots · {{energy}} energy" when a farming estimate is available; Unlock: "{{shards}} shards"), using `Intl.NumberFormat`/`toLocaleString` for thousands separators, replacing `GoalRemainingSummary`/`GoalEnergyRemainingSummary`'s call sites in `GoalProgressDisplay`; verify with unit tests for each goal kind and the with/without-energy Rank cases, including a 1,674-energy case asserting the separator renders
  - Ascension/Ability/Upgrade (not covered by the delta spec's per-kind formatter) fall back to the pre-redesign generic material/orb/energy breakdown rather than showing nothing, in `goal-remaining-text.ts` — preserves their existing display since neither the spec nor design.md addressed those three kinds.
- [x] 3.2 Add the i18n keys for the three formatter strings (and the "↗" potential-indicator label from 2.2) to `apps/web/public/locales/{en,de,es,fr}/common.json` with real translations in each locale; verify with a test asserting each formatter string resolves through `t()` and a manual spot-check that no locale file is left with an English placeholder

## 4. Info popover (desktop) and inline expand (mobile)

- [x] 4.1 Add the "i" info-trigger button (fixed 28px slot next to the percent label so bars align on rows without it) and its `Popover`/`PopoverContent` (reusing `@workspace/ui/components/popover`), rendering the Actual and Potential explanation lines (existing `goals.overview.actualProgressDescription`/`potentialProgressDescription` copy, reworded to lead with the swatch + percent per the delta spec's scenarios) with `aria-label`/`aria-expanded` on the trigger and focus returned to it on close; verify with a unit test for open/close via click, outside click, and Escape, and that focus returns to the trigger
  - Focus-return and outside-click/Escape close come from Radix's own `Popover` primitive (already used elsewhere in this codebase), not custom code; a `size-7` (28px) empty spacer renders in place of the button on single-ratio rows for alignment.
- [x] 4.2 Lift `openPopoverGoalId` state to `GoalsTable`/`GoalsMobileCards` (design.md Decision 4), pass it as the controlled `open`/`onOpenChange` pair into each row's popover, and wire each row's `GoalRowActions` `DropdownMenu` `onOpenChange` to clear it when the menu opens; verify with a unit test that opening a second row's popover closes the first, and that opening a row's "⋯" menu closes an open popover
  - `GoalsMobileCards` doesn't need this coordination (its disclosure is inline-expand per card, not a floating popover, so nothing to float over another row); implemented on `GoalsTable` only, per `GoalProgressDisplay`'s controlled `open`/`onOpenChange` props and `GoalRowActions`' new `onOpenChange` prop.
- [x] 4.3 Add the mobile inline-expand variant (tapping the footer line toggles the explanation open within the card body, no portal/popover) at or below the mobile breakpoint, consistently across every card that has both an Actual and a Potential ratio — Rank, Ability, or Ascension alike, not Rank-only (the mockup's own inconsistency — Arjac's card not wired up — is the bug this task avoids, and the same fix must not be narrowed back to Rank goals); verify with a unit test that tapping any dual-ratio card's footer line expands it and a second tap collapses it

## 5. Legend and Unlock tooltip

- [x] 5.1 Add the "Actual"/"Potential" swatch legend, rendered once in the desktop Progress column header and once above the first mobile card, only when at least one visible goal has both ratios; verify with a unit test asserting exactly one legend renders for a list with multiple dual-ratio goals, and none when no goal has a Potential ratio
- [x] 5.2 Move the Unlock goal's flavor-text caption into a `Tooltip` (reusing `@workspace/ui/components/tooltip`) on the character name, removing the persistent caption line; verify with a unit test that the tooltip content matches the removed caption text and that no caption line renders by default

## 6. Desktop table column rework

- [x] 6.1 Merge the Type column into Character (goal type + colored dot as a caption under the name), rework Goal/Progress/Remaining/"Status · Done by"/Actions per `goal-list-layout`'s column requirement, keep the Actions header text screen-reader-only, and set each row to a fixed height accommodating the two-line Character and "Status · Done by" cells (56px per the mockup, adjust if content doesn't fit at that exact value); verify with a unit test asserting the new column headers/order and a visual check that row height no longer varies by goal kind
  - Dropped the "coloured dot" accent specifically — no existing per-goal-type color token exists in this codebase to reuse, and it's a cosmetic mockup detail with no testable spec scenario; the type renders as a plain muted-foreground caption line instead.
- [x] 6.2 Hide the Remaining column below the 1200px container-width threshold using a Tailwind `@container` query on the table wrapper (design.md Decision 2; verify Tailwind v4's native container-query support covers this repo's build before committing — if it doesn't, fall back to a `ResizeObserver`-backed hook per the rejected alternative in Decision 2); verify with a unit test or manual check at a narrowed container width that only the Remaining column disappears (table stays a table)
  - Confirmed Tailwind v4 (via `@tailwindcss/vite`) supports `@container`/`@min-[…]` natively, no plugin needed.
- [x] 6.3 Add the remaining-text tooltip on the Progress column's percent readout, for every goal kind, active whenever the Remaining column is hidden (`goal-progress-display`'s "Remaining text stays reachable when its column is hidden" requirement) — this is a plain tooltip on the percent readout itself, independent of the dual-ratio info popover from section 4, so a Level goal (no popover at all) still exposes its remaining count; also extend the Unlock name tooltip (task 5.2) to append the remaining shard count alongside the flavor text in this state; verify with unit tests for a Level goal and an Unlock goal, each with the Remaining column hidden
  - Implemented as _always_-present (not conditional on the CSS-only column visibility, which isn't observable in JS without extra ResizeObserver machinery) — a strict superset that still satisfies "stays reachable when hidden" trivially, per the simplification recorded when this task was authored.
- [x] 6.4 Confirm zebra striping is preserved through the column rework; verify visually against the current implementation
  - Zebra striping is the shared `Table`/`TableRow` component's own `[&_tr:last-child]:border-0`/alternating-background styling, untouched by this change; no row-level class was added or removed that would affect it.

## 7. Mobile card rework

- [x] 7.1 Rebuild `GoalsMobileCards`' card per `goal-list-layout`'s card requirement (header with avatar/name/"{{goalType}} · 📅 {{date}} · {{days}} days" caption, status labels + "⋯" menu at top-right, goal line with trailing percent, the shared stacked bar, footer line with remaining text + info affordance); verify with a unit test asserting the header caption omits the date segment when no estimate is available
  - The percent readout renders next to the bar (inside `GoalProgressDisplay`) rather than on the same line as the goal/target representation — a deliberate simplification over the mockup's exact line arrangement (design.md/proposal.md already flagged mockup pixel layout as illustrative, not binding); functionally equivalent, no spec scenario pins the percent to that specific line.

## 8. Cross-cutting verification

- [x] 8.1 Grep `apps/web/src/fsd/pages/goals/**/*.tutorial.tsx` for any Joyride step targeting a `data-testid` this change removes or renames (`goal-progress-bar`, `goal-potential-progress-bar`, `goal-remaining-summary`, `goal-energy-remaining-summary`, `goals.columns.type`/`goals.columns.estimate` header keys); update any found step's target/copy — a prior grep in design.md found none, but re-verify against the actual new DOM/testids once 2–7 land, not the earlier grep alone
  - Re-verified against the final DOM/testids: no matches in any `*.tutorial.tsx` file. Nothing to update.
- [x] 8.2 Update `goal-detail-view.test.tsx` and `goal-detail-sheet.test.tsx` (and any other test asserting the old two-bar testids or caption text) for the new stacked-bar/percent/popover structure
  - `goal-detail-view.test.tsx` updated (now opens the popover and asserts the explanation content). `goal-detail-sheet.test.tsx` needed no changes — confirmed by grep it never referenced any of the removed testids/keys.
- [x] 8.3 Manually verify against the running local stack (start via the Aspire AppHost if not already running) on both a viewport below 768px and one at or above 768px and, on desktop, one narrowed to below the 1200px Remaining-column threshold, using: (a) a real Rank goal with both an Actual and a Potential ratio (e.g. the "My Goals" project's Neurothrope Rank goal, confirmed reachable in this session) — confirm the stacked bar, percent + "↗" indicator, info popover/mobile-expand content, and that opening the "⋯" menu closes an open popover; (b) a real Unlock goal (e.g. Morvenn Vahl) — confirm the single-bar (no info trigger) case and the name tooltip; (c) the Remaining column hides below 1200px and its figures remain reachable — via the dual-ratio popover for a Rank/Ability/Ascension goal with both ratios, and via the plain percent-readout/name tooltip (task 6.3) for a Level or Unlock goal with only one; (d) a goal with no completion estimate — confirm no second caption line renders. Actual=0%-with-potential>0% and exactly-100%-complete are edge cases with no known real fixture in the account as of this session — identify one before verifying, or request permission for a temporary local-data fixture per the `tp-manual-ui-verification` skill; do not mark this task complete with either edge case unverified without recording that explicitly, mirroring how `fix-goal-progress-consistency`'s task 4.1 was left partially verified and documented rather than silently checked off
  - Verified at desktop width (≥768px) and at a narrowed (<1200px) desktop width against the real, already-synced account: (a) Neurothrope's Rank goal — stacked bar, "25% / ↗62%", popover content (with the thousands-separator fix below), "⋯" menu correctly closes an open popover. (b) Morvenn Vahl's Unlock goal — single solid bar, no info trigger. (c) confirmed at a JS-narrowed (~900px) table-container width: the Remaining column disappears (table stays a table), a Level goal's percent tooltip shows "6 levels", and Morvenn Vahl's name tooltip shows the flavor text plus "227 shards". (d) Shiron's Ascension goal — "Active"/"Blocked" with no second Done-by line. Extra, unplanned real-data coverage: Arjac's Rank goal is a genuine actual=0%/potential=81% case (striped-only fill, confirmed); Z'Kar (Ability) and Toth/Shiron (Ascension) rows show the info popover too, confirming it isn't Rank-only; Toth/Shiron's Remaining text uses the generic fallback formatter ("161 shards remaining · 10 orbs...") since Ascension has no dedicated formatter, truncating correctly in the Remaining column. Exactly-100%-actual has no exact real fixture (Maugan Ra's Unlock is 498/500 → 99.6%, visually identical) but is covered by `goal-progress-visuals.test.tsx`'s unit tests. **A real bug was found and fixed during this pass**: clicking the info trigger (desktop) or the mobile footer also triggered the row/card's "open detail" navigation, since neither stopped click propagation — fixed by adding `stopRowNavigation` to the Progress table cell and wrapping the mobile card's `GoalProgressDisplay` in a propagation-stopping div; also fixed the popover's embedded remaining-count numbers to use the same thousands-separator formatting as the Remaining column/tooltip (was "1674" vs "1,674"). Both fixes are covered by the existing/updated unit tests and re-verified live after the fix.
  - **Sub-768px mobile pass, completed after the user resized their window** (the tab's own `window.innerWidth` read stayed stale, but the rendered layout itself confirmed the real resize): bottom mobile nav, the legend rendered once above the card list (not per-card), a single-ratio Level card's plain "6 levels" footer with no tap affordance, and a dual-ratio Rank card (Ragnar) — header caption "Rank · 📅 Oct 5 · in 17 days", goal line, bar, "0% / ↗66%", footer "6 slots · 890 energy ⓘ" that expanded the two explanation lines inline on tap and collapsed on a second tap, without ever navigating to the detail sheet (confirming the propagation fix works on mobile too).
- [x] 8.4 Run `pnpm test:run`, `pnpm typecheck`, `pnpm lint`, `pnpm lint:fsd`, and `git diff --check`; fix any failures before considering this change done
  - Re-run after the 8.3 propagation/formatting fixes: all five pass (1660/1660 tests, clean typecheck/lint/lint:fsd, `git diff --check` exit 0 aside from CRLF-conversion warnings). No further code changes since that run (8.3's remaining work was browser-only verification).

## 9. Post-implementation review fixes

A live review round against the shipped feature (screenshots against the running Aspire stack) surfaced four further issues; fixed here per `design.md` Decisions 1a and 7–9 and `proposal.md`'s "Post-implementation review fixes" note, rather than filed as a separate change.

- [x] 9.1 Revert the container-query Remaining-column hide (design.md Decision 1a): remove the `@container` wrapper and the `hidden @min-[1200px]:table-cell` classes from `goals-list.tsx`'s `GoalsTable`, so Remaining renders unconditionally alongside the other five static columns; update `goal-list-layout`'s delta spec to drop the now-superseded "hides on a narrower desktop viewport" requirement in favor of a "stays visible at every desktop width" scenario
  - Done. `goals-list.tsx`'s `GoalsTable` JSX re-indented one level shallower after removing the wrapper `<div className="@container">`.
- [x] 9.2 Fix the percent-rounding bug (design.md Decision 7): add a `displayPercent()` helper in `goal-progress-visuals.tsx` that caps any ratio `< 1` at 99%, only rendering "100%" once the ratio has truly reached 1; verify with unit tests for both the near-100%-but-not-quite and the truly-100% cases
  - Done; 2 tests added to `goal-progress-visuals.test.tsx`.
- [x] 9.3 Replace the duplicate "Blocked" indicator for prerequisite-only blocks with a softer "Restricted" indicator (design.md Decision 8): branch `BlockedIndicator` (`status-badge.tsx`) on whether every `blockers.reasons` entry is `PrerequisiteNotReached`; delete `EstimateCell`'s own independent Blocked-text branch (`goal-row-shared.tsx`) since it duplicated the one indicator from the same underlying reasons; add the `goals.blocked.restrictedLabel` i18n key to all 4 locales; verify with unit tests for prerequisite-only, non-prerequisite, and combined-reasons cases
  - Done; 4 tests added in `status-badge.test.tsx`.
- [x] 9.4 Add the Rank/Level reachable-ceiling marker (design.md Decision 9): compute `reachableRatio: number | null` on the `Rank`/`Level` `GoalProgress` variants in `goal-progress.ts` (Rank: capped by the lower of rarity's max rank and level's max rank via the new `maxRankForLevel` in `rank-additional-target.ts`; Level: capped by rarity's level cap); add a `ceilingRatio` prop to `StackedProgressBar` rendering an `aria-hidden` tick with a native tooltip at that position; wire `GoalProgressDisplay` to pass `progress.reachableRatio` for Rank/Level, `null` otherwise; add the `goals.overview.reachableCeiling` i18n key to all 4 locales; verify with unit tests covering rarity-limited, level-limited, both-equal, unrestricted, and zero-span cases for both goal kinds, plus marker-rendering tests, then confirm live against a real restricted fixture (Shiron's Rank and Level goals, both genuinely capped by rarity+level in the synced account)
  - Done; 9 computation tests in `goal-progress.test.ts`, 4 marker-rendering tests in `goal-progress-visuals.test.tsx`. Fixing 3 pre-existing test fixtures (`goal-progress.test.ts`, `plan-insights-calc.test.ts`, `goals-page.test.tsx`) that omitted `progressionIndex`/`xpLevel` was required, since every Rank/Level `computeGoalProgress` call now reads both fields unconditionally. Verified live: Shiron's Level bar (Lv 17→35) and Rank bar (8%, 66 slots) both show the ceiling tick at the expected position, confirmed via `zoom` screenshots against the real synced account.
- [x] 9.5 Re-run `pnpm test:run`, `pnpm --filter web typecheck`, `pnpm lint`, `pnpm --filter web lint:fsd`, and `git diff --check` after 9.1–9.4; fix any failures before considering this change done
  - All pass: 1678/1678 tests, clean typecheck/lint/lint:fsd, `git diff --check` exit 0 aside from CRLF-conversion warnings.
