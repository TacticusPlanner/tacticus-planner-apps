## 1. Eligibility engine

- [x] 1.1 Extend `campaign-event-eligibility.ts` (`availableCampaignBattles`) with a standing-campaign reached check: a battle is eligible when its `battleIndex` is at most one past its `{campaignGroupId, type}` track's `highestCompletedBattleIndex` (from `campaign-progress`), and a track with no `campaign-progress` entry only admits `battleIndex: 0` — keep the existing event-campaign gate unchanged. Verify with unit tests covering: a reached standing battle stays eligible, an unreached one is excluded, a track with no progress entry only admits `battleIndex: 0`, a standing challenge battle is gated the same as a non-challenge one, and event-campaign behavior is unchanged by this change.
- [x] 1.2 Fetch the `campaign-progress` chunk in `use-daily-raids.ts` (`getCampaignProgress`, already used by `campaigns-page.tsx`) and pass it into `availableCampaignBattles` alongside the existing `campaign-events-progress` fetch. Verify with a test that a locked standing battle no longer appears in the map passed to `calculateDailyRaids`.

## 2. Keep real-attempt tracking independent of eligibility

- [x] 2.1 In `use-daily-raids.ts`, build a second, unfiltered battle map (full catalog, no `availableCampaignBattles` filtering) and rebuild `locationsByBattleId`, `buildBattleAttemptIndex`, `calculateRealEnergyUsedToday`, `buildAttemptsLeftByBattle`, and `buildTodaysAttempts` from it instead of the eligibility-filtered map; only `calculateDailyRaids`'s `battlesById` parameter keeps the filtered map. The two progress-map builders were also extracted into `campaign-event-eligibility.ts` (`buildCampaignEventProgressByKey`/`buildCampaignProgressByKey`) to keep `use-daily-raids.ts` under the repo's 400-line file cap. No dedicated `use-daily-raids.ts` test harness exists in this codebase to add an isolated hook-level unit test for the map split itself; correctness relies on (a) `daily-raids-energy.ts`'s existing pure-function tests, unaffected by this change, (b) the delta spec's "real attempt still counts" scenario, and (c) manual verification (3.2).
- [x] 2.2 Ran the full `pnpm test:run` suite (1827 tests, 235 files, all passing) including `daily-raids-energy.test.ts` and `campaign-event-status.test.tsx` — one pre-existing fixture in `campaign-event-status.test.tsx` needed a `battleIndex: 0` added (it predates this change's new required field); no other regressions.

## 3. Verification

- [x] 3.1 Ran `pnpm test:run` (1827/1827 passing), `pnpm typecheck` (clean), `pnpm lint` (clean, after extracting the two builders to stay under the 400-line file cap), `pnpm lint:fsd` (clean), and `git diff --check` (only CRLF-normalization notices, no real whitespace errors).
- [ ] 3.2 Manually checked against the running local stack (an already-authenticated tab from this same session) at `/dailies/raids/today`: the page renders correctly with real schedule data (energy/attempts totals, campaign-event status block, standing-campaign node cards all correct), zero console errors before or after this change. **Could not verify the locked-node exclusion itself (a, b)**: this account's `/progress/campaigns` shows 100% completion on every standing campaign (460/460 normal, 460/460 mirror, 8/8 cleared) — it has no locked standing node in its own data, so there is nothing for the new gate to exclude. (c) is the same limitation. Viewport check not performed (blocked on the same missing locked-node case, not a viewport concern).
- [ ] 3.3 **Unverified, not silently skipped**: the connected account's campaigns are 100% complete, so there is no in-progress challenge-battle data available in this session to confirm a challenge battle's `battleIndex` advances the same way as its track's regular battles. This assumption (design.md) should be checked against an account with incomplete standing-campaign progress before treating PLAN-01/02 as fully closed for the challenge-node case.
