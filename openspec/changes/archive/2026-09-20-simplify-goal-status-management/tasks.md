## 1. Primary pause/resume action

- [x] 1.1 Add a primary pause/resume icon button to `GoalRowActions`'s Actions cell, before the "⋯" trigger: an `Active` goal shows a control that pauses it, a `Paused` goal shows a control that resumes it, `Completed`/`Archived` goals show neither. Remove the corresponding pause/resume `DropdownMenuItem`s from the "⋯" menu (now redundant with the primary control). Verify with a component test covering all four statuses.
- [x] 1.2 Reused the existing `goals.actions.pause`/`goals.actions.resume` keys as the new control's accessible label (they're no longer used as menu-item text, so no wording conflict) — no new i18n keys needed for this task specifically.

## 2. Archive gated to Reached

- [x] 2.1 Add a `reached: boolean` prop to `GoalRowActions`; hide the "⋯" menu's Archive item when `reached` is `false`. Keep Unarchive always offered for an already-`Archived` goal regardless of `reached`. Verify with a component test: unreached + not archived → no Archive item; reached + not archived → Archive item present; archived (either reached value) → Unarchive item present.
- [x] 2.2 Thread `reached={isReached(row.goalId)}` from `goals-page.tsx` and `project-detail-page.tsx` into `GoalRowActions` at both call sites (desktop rows and mobile cards go through the same component, so one prop covers both).

## 3. Prerequisite cascade

- [x] 3.1 In `use-goal-actions.ts`, add a cascade-aware pause/resume entry point that accepts the acting goal's `dependsOn` ids, each id's current status (to exclude `Completed`/`Archived` prerequisites before any other check — see design.md), and a `Map<string, number>` of "how many rows in the current set list this id as a dependency." Pausing: cascade-pause only surviving ids with a dependent count of exactly 1 (this goal being the sole dependent). Resuming: cascade-resume every surviving id in `dependsOn` unconditionally. Verify with unit tests covering: sole-dependent prerequisite pauses with its dependent; shared-dependent prerequisite (count > 1) does not pause; resume cascades regardless of shared count; a `Completed` prerequisite and an `Archived` prerequisite are both excluded from the cascade in either direction.
- [x] 3.2 Change `useGoalActions`'s `pendingId: string | null` to `pendingIds: ReadonlySet<string>`, and run the acting goal's call followed by each surviving cascade target's call sequentially (not `Promise.all` — see design.md's `NormalizeAsync`-ordering rationale), tracking every affected row's own pending state. On completion, show one toast reporting overall success, or how many of N calls succeeded if any failed partway — do not roll back the calls that already landed. Verify with unit tests covering: all calls succeed → one success toast, no pending ids remain; one cascade call fails → toast reports partial success, the failed row's status is unchanged while the succeeded ones reflect their new status.
- [x] 3.3 Build the dependent-count `Map` once per render in `goals-page.tsx` and `project-detail-page.tsx` from `allRows` (not per row), and pass it plus each row's `dependsOn`/status into the new cascade entry point from `GoalRowActions`'s primary pause/resume control (task 1.1). **Known limitation, not solved here** (design.md): on Project Detail, the dependent count only considers that project's own rows — a prerequisite also depended on by a goal in a different project is not detected as shared and could be cascade-paused. Left as-is per design.md's accepted trade-off.

## 4. Bulk pause/resume

- [x] 4.1 Add "Pause all goals" and "Resume all goals" items to `project-detail-header.tsx`'s existing overflow menu, calling `updateProjectGoalsStatus(projectId, status)` via a new `setGoalsStatus` method on `useProjectActions` (whose doc comment already claimed to cover "bulk pause/resume" — this fulfills it). Disabled while a call is in flight; toasts on success ("N goals paused/resumed") and failure, matching the existing pattern. Verified with unit tests on `setGoalsStatus` (`use-project-actions.test.ts`) and a component test asserting both menu items call the client function with the current project id and the right target status (`project-detail-page.test.tsx`).
- [x] 4.2 Added i18n keys for the two new menu items and their success toast copy (with `_one`/`_other` pluralization), with real en/de/es/fr translations. The failure toast reuses the existing generic `goals.toasts.actionError` key, already translated in all four locales.
- [x] 4.3 Confirmed `updateProjectGoalsStatus` was already cleanly exported from `entities/project`'s index — no new export needed. `project-detail-header.tsx` itself doesn't call it directly; it goes through `useProjectActions` (a `features/project-management` hook), matching this repo's existing pattern for that header's other project mutations (`activate`, `save`).

## 5. Layout requirement sync

- [x] 5.1 No component change needed beyond 1.1. Verified by running the full `pages/goals` test suite (410 tests across 45 files, including `goals-list.test.tsx` and `goals-mobile-cards`-adjacent coverage) unmodified except for the `pendingId` → `pendingIds` stub fix — all pass with the new primary control present.

## 6. Verification

- [x] 6.1 Ran `pnpm test:run` (236 files / 1847 tests, all passing), `pnpm typecheck` (clean), `pnpm lint` (clean — fixed three `react-hooks/exhaustive-deps` warnings surfaced by the new `useMemo`s in `goals-page.tsx` by memoizing `nonArchivedGoalIds`/`nonArchivedRows`/`archivedRows` properly rather than leaving the warnings), `pnpm lint:fsd` (clean), and `git diff --check` (CRLF-normalization notices only, no real whitespace errors).
- [ ] 6.2 Manually verified against the running local stack (an already-authenticated tab from this session), live, on the real account used throughout this session: **(c) confirmed both directions** — an unreached goal's "⋯" menu shows no Archive item; switching to the account's one Reached goal (Maugan Ra, 100%) shows Archive. Primary pause/resume also confirmed live end-to-end (paused then resumed Bellator; status badge, icon, and a "Goal updated." toast all updated correctly, zero console errors throughout). **(d) UI-only**: the header's "Pause all goals"/"Resume all goals" items render correctly, in the right place, correctly disabled/separated from the pre-existing project-level Archive item — did not click through, since this project's real 32 goals would need un-pausing afterward and the underlying call is already covered by automated tests. **(a), (b), (e) not verified live**: this account's real goals have no `dependsOn` relationships between them in the areas checked, so no cascade could be observed to fire; relying on the automated unit/component test coverage (task 3.1) for these. **Viewport check attempted, inconclusive**: `resize_window` reported success but the screenshot kept rendering at desktop width — likely a tool limitation, not a real narrow-viewport render. `GoalsMobileCards` passes the exact same `reachedByGoalId`/`cascadeContext` props to the same `GoalRowActions` component as the desktop table (same code path, no platform branching for this feature), and the full test suite's mobile-card tests pass, but this was not visually confirmed live at a real narrow viewport.
- [ ] 6.3 Not performed — depends on 6.2's (a)/(b) cascade cases actually firing, which didn't happen live (no real `dependsOn` data to trigger a cascade). ORDER-02 remains reopened and unverified either way (see CLUSTERS.md); this task's own cascade-amplification angle on it also remains unchecked.
