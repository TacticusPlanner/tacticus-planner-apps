## 1. Implementation

- [x] 1.1 In `userjot-provider.tsx`, add an exported `USERJOT_BOARD_URL = "https://tacticusplanner.userjot.com"` constant — verify by reading the updated export. (`UserJotOpenTarget`'s `to` union was briefly widened to include `"notifications"` for task 1.5's original approach, then reverted along with it — see 1.5.)
- [x] 1.2 Create `apps/web/src/fsd/app/providers/userjot-board-link.tsx` exporting `UserJotBoardLink`: an icon-button-styled `<a>` (or `Button asChild`) with `href={USERJOT_BOARD_URL}`, `target="_blank"`, `rel="noopener noreferrer"`, an `ExternalLink` icon (lucide-react), `aria-label={t("feedback.viewBoard")}`, and `data-testid="userjot-board-link"` — matching `UserJotFeedbackButton`'s existing `variant="outline" size="icon"` shape so it sits naturally beside it. Verify by reading the new component.
- [x] 1.3 In `desktop-layout.tsx`, render `UserJotBoardLink` beside `UserJotFeedbackButton` in the header controls row (`data-testid="desktop-header-controls"`) — verify by reading the updated JSX.
- [x] 1.4 In `auth-control.tsx`, render `UserJotBoardLink` (or an equivalent full-width row using the same `data-testid`) beside the existing `data-testid="auth-feedback"` Feedback button in the mobile drawer, closing the drawer (`setMenuOpen(false)`) on activation the same way the Feedback button does — verify by reading the updated JSX.
- [x] 1.5 In `userjot-feedback-button.tsx`, click handler stays `open()` unconditionally. Originally implemented as `open(unreadCount > 0 ? { to: "notifications" } : undefined)`, then reverted after task 2.2's live verification found the notifications view doesn't reliably reflect the unread indicator's source (`design.md`'s Decisions/Risks) — verify by reading the current handler.
- [x] 1.6 In `apps/web/index.html`, add `whispers: false` and `notifications: { updates: 'none' }` to the `widget` object in the `uj.init()` call, alongside the existing `launcher: false` — verify by reading the updated script tag against UserJot's documented "disable all default UI" combination.

## 2. Tests

- [x] 2.1 Create `userjot-board-link.test.tsx` asserting the link renders with the correct `href`, `target="_blank"`, and `rel="noopener noreferrer"` — verify with `pnpm --filter web test:run userjot-board-link`.
- [x] 2.2 Manually verified against the real widget (Aspire stack, signed-in account with genuine unread activity): clicking the feedback button routed to `{ to: "notifications" }` did NOT open a populated view — first attempt reported "We couldn't load this page," retrying showed "You're all caught up" despite the account's unread indicator correctly showing. Re-checking UserJot's SDK reference confirmed why: the `unread` count combines conversation replies, changelog posts, and feedback notifications with no per-type breakdown, so a fixed "notifications" target is wrong whenever the source was a changelog post. Documented in `design.md`'s Decisions/Risks; task 1.5's routing was reverted as a result. This task is complete as an investigation — the finding is the resolution, not a blocker.
- [x] 2.3 Updated `userjot-feedback-button.test.tsx`: kept a single "opens the widget's default view on click, regardless of unread activity" test asserting `open` is called with no arguments — the conditional `{ to: "notifications" }` test was removed along with the reverted routing (task 1.5/2.2) — verify with `pnpm --filter web test:run userjot-feedback-button`.
- [x] 2.4 Add or update a test in `desktop-layout.test.tsx` and `auth-control.test.tsx` confirming `UserJotBoardLink` renders in both the desktop header and the mobile drawer — verify with `pnpm --filter web test:run desktop-layout auth-control`.
- [x] 2.5 Signed-in half verified live (existing authenticated session on the already-running local stack, real unread indicator present): no popup appeared unprompted after an extended wait; the widget only opened on an explicit click of the feedback button, landing on the default view (which surfaced the actual changelog post driving the unread indicator — see `design.md`'s Risks). Unauthenticated-landing-page half NOT live-verified — the connected browser only has an authenticated session, and forcing a sign-out to test the logged-out landing page wasn't done without checking with the user first. Static confidence remains high: `index.html`'s `whispers: false` + `notifications: { updates: 'none' }` additions match UserJot's own `init()` reference's documented "disable all default UI" combination exactly, alongside the pre-existing `launcher: false`.

## 3. i18n

- [x] 3.1 Add `feedback.viewBoard` ("View full board" or similar) to `apps/web/public/locales/en/common.json`, and real de/es/fr translations of it to the corresponding locale files at the quality of the sibling `feedback.button` key — this is part of the task, not a placeholder to translate later.

## 4. Gates

- [x] 4.1 Ran `pnpm test:run` (245 files, 1938 tests, all passing), `pnpm typecheck`, `pnpm lint`, `pnpm lint:fsd`, and `git diff --check` — all passed.
