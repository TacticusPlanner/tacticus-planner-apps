## 1. Catalog package: schema, portraits, faction icons

- [x] 1.1 Tighten `packages/game-catalog/src/schemas/npc.ts` to require `factionId: string`, `alliance: string`, and `kind: z.enum(["unit","machineOfWar","object"])`, and verify `schemas.test.ts` covers a valid record, a missing `kind`, and an unknown `kind` value.
- [x] 1.2 Generate `packages/game-catalog/src/game-entities/npc-portrait-overrides.ts` (variation id → bare portrait file name) from V1 `tacticusplanner/src/fsd/4-entities/npc/data/new-npc-data.json`, add `icons/npc.ts` exporting `npcPortrait(id): string | undefined`, export it from the package index, and verify `icons/npc.test.ts` resolves `necroBossWarden` → `…/characters/ui_image_portrait_necro_warden_01.png` and returns `undefined` for an unknown id.
- [x] 1.3 Copy `ui_image_portrait_adept_warsuit_01.png` and `ui_image_portrait_darka_watcher_01.png` from V1 `src/assets/images/snowprint_assets/characters/` into `apps/web/public/game_catalog/characters/`, and verify a test asserts every file referenced by `npc-portrait-overrides.ts` for a `kind: unit` id in the real dataset exists on disk (loot/power-up files excluded).
- [x] 1.4 Copy V1 `src/assets/images/factions/*.png` to `apps/web/public/game_catalog/factions/<FactionId>.png` using the V1 `faction.icon.tsx` display-name → id mapping, add `factionIcon(factionId)` to `icons/` (exported from the package), and verify `icons/faction.test.ts` resolves `Sisterhood` and `Tau` and that every `factionId` in the served `npcs` fixture (except `Objects`) has a file.
- [x] 1.5 Add `traitIconOverrides` entries for the NPC-only traits V1 mapped explicitly (`Boss` → `boss_adjutant`, `Unstoppable` → `mounted`, `GetStuckIn` → `beast_snagga`) and verify `icons/trait.test.ts` no longer resolves those three to `unknown` and the referenced files exist under `game_catalog/traits/`.

## 2. i18n: `npcs` namespace and `library` copy

- [x] 2.1 Generate `apps/web/public/locales/{en,de,es,fr}/npcs.json` (variation id → localized name) from V1 `src/fsd/5-shared/l10n/data/<locale>/npc_names.json` (`name`, falling back to the served catalog name when V1's is empty), register `npcs: Record<string, string>` in `shared/config/i18n/i18next.d.ts` and the namespace list in `i18n.ts`, and verify a new `npc-translations.test.ts` asserts all four locales have the same key set and no empty values.
- [x] 2.2 Add `library.json` keys for the page in all four locales — `npcs.title`, filter labels/placeholders (search, faction, damageType, trait, clearFilters, filtersCount), selector labels (`npcs.variation`, `npcs.level`), variation mode labels (`npcs.variationMode.standard|lhe|legendary|survival|campaign|championEvent|tutorial|leviathan|kronos|gorgon|syncPvp|unknown`), detail labels (`npcs.stats.health|armour|damage|movement`, `npcs.attacks.melee|ranged|hits|range`, `npcs.traits`, `npcs.noTraits`), and state copy (`npcs.noMatchingNpcs`, `npcs.noMatchingVariation`, `npcs.loadFailed`) — with real de/es/fr translations at the quality of the sibling `raidBosses.*` keys, and verify `library-translations.test.ts` is extended to assert every new leaf key exists and is non-empty in every locale.

## 3. `entities/npc` slice

- [x] 3.1 Create `apps/web/src/fsd/entities/npc/` with `model/types.ts` (`NpcGroup`, `NpcVariation`, `NpcLevelOption`), `lib/npc-slug.ts`, `lib/build-npc-groups.ts` (kind filter, all-zero availability, grouping by `name`, slug, default variation = shortest available id then served order), and a public `index.ts`; verify `build-npc-groups.test.ts` covers: the five-variation Makhotep fixture → one group with default `necroNpcWarden`; `kind` exclusion of a MoW and an object; an all-zero variation hidden while its group stays; a group with only all-zero variations dropped; slug of `Sy-gex` and `From Golden Light Power-up`; and a slug-uniqueness assertion over the real served fixture.
- [x] 3.2 Add `lib/order-levels.ts` (rank → stars, stable; `servedIndex`; `tie` flag when rank+stars repeat within the variation) and verify `order-levels.test.ts` with the `necroBossWarden` order fixture (rank 2/2★ served before 1/2★ displays after it, `servedIndex` preserved) and a three-way tie fixture (`blackNpc4HavocSurv`) flagging all three.
- [x] 3.3 Add `lib/npc-filters.ts` (`matchesNpcFilters`, search over localized name, faction, all-of damage types across melee/ranged/ability arrays, all-of traits) and verify `npc-filters.test.ts` covers each filter alone, combined, the cross-variation "group listed if any variation matches" rule, and the empty-filter identity.
- [x] 3.4 Add `lib/variation-mode.ts` (ordered suffix registry → `library:npcs.variationMode.<key>`, `standard` for the group default with no suffix, `unknown` otherwise) and verify `variation-mode.test.ts` maps `necroBossWardenLHE`, `admecBossDominusCE`, `necroNpc1TutWarriorFTUEtest`, `tyranNpc4WarriorCE2Gorgon`, `astraNpc4Mortar_SyncPvp_Kronos`, and an unrecognised id.
- [x] 3.5 Add `lib/use-npc-labels.ts` (`t("npcs:"+id, { defaultValue: catalogName })`) and `ui/npc-portrait.tsx` (portrait via `npcPortrait(id)`, initials fallback, load-error fallback), and verify `npc-portrait.test.tsx` renders the image when a src resolves and initials when it does not.

## 4. Page: orchestrator, desktop, mobile

- [x] 4.1 Create `pages/library/ui/npcs/npcs-page.tsx` (orchestrator): `useLiveQuery(getNpcs)` via `hooks/use-npcs-catalog.ts`, `buildNpcGroups`, page-local filter state, `useLibraryRouteSelection` with the listed group slugs, `variation`/`level` query resolution with the default rules (no write-back of defaults), navigation handlers that drop `level` on variation change and both on NPC change, the "selected variation stops matching → first matching" effect, and `npcs-page.view-model.ts` defining `NpcsPageViewProps`; verify `npcs-page.test.tsx` (mocked `@workspace/game-catalog/queries`) covers: direct link restore, NPC change resets query, variation change drops `level`, unknown variation → default without URL change, out-of-range level → first, collection URL → first group redirect, unknown group → first group replace, loading shows no redirect, and load failure state.
- [x] 4.2 Build the shared detail pieces under `pages/library/ui/npcs/`: `npc-detail-header.tsx` (portrait, name, faction icon + name, rank/stars icons, Variation `Select` with mode label + secondary id, disabled when single; Level `Select` with stars/rank icons and health on ties), `npc-stat-cards.tsx`, `npc-attack-rows.tsx` (melee row, ranged row with range), `npc-traits.tsx` (icon + Radix tooltip with localized name; Library empty-state when none), and verify `npc-detail.test.tsx` covers stats following the level, melee-only vs melee+ranged rows, tie options showing health, single-variation disabled select, and the empty traits state.
- [x] 4.3 Build `npcs-filter-bar.tsx` (search input, faction `Select`, damage-type and trait multi-select comboboxes, clear-filters button with active count) and verify `npcs-filter-bar.test.tsx` covers emitting each filter change, the count badge, and clear.
- [x] 4.4 Build `desktop/npcs-desktop-page.tsx` (filter bar; two-column: `npc-list.tsx` tile grid with `aria-pressed` beside the detail panel; no-matching-NPCs state with clear control; no-matching-variation state in the detail column) and verify `npcs-desktop-page.test.tsx` at 1280px renders the tile list beside the detail, the selected tile is pressed, and both empty states render.
- [x] 4.5 Build `mobile/npcs-mobile-page.tsx` (`npc-combobox.tsx` on the `shared/ui/unit-combobox` Popover + Command primitives with portrait/name/faction rows and search; collapsible filters with count; stacked detail with full-width selectors and 2×2 stat grid) and verify `npcs-mobile-page.test.tsx` at 390px renders the combobox and no tile list, typing `makh` narrows to Makhotep, selecting navigates to `/library/npcs/makhotep`, and the layout has no horizontal overflow.
- [x] 4.6 Wire the route: replace both `npcs` entries in `pages/library/route.tsx` with the lazy `NpcsPage`, narrow `LibraryCollectionPage`'s `collection` prop to `"machines-of-war"`, and verify `route.test.tsx` and `library-collection-page.test.tsx` are updated (NPC placeholder assertions removed; MoW placeholder unchanged).

## 5. Tour

- [x] 5.1 Add `pages/library/ui/npcs/npcs.tutorial.tsx` exporting `useNpcsTutorial(): TourPageSteps` with desktop steps (`npcs-filter-bar`, `npcs-list`, `npcs-variation-select`, `npcs-level-select`, `npcs-stats`) and mobile steps (`npcs-combobox`, `npcs-variation-select`, `npcs-level-select`, `npcs-stats`), registered via `useTourPageSteps` in the orchestrator, and verify `npcs.tutorial.test.tsx` asserts every step target exists on the rendered desktop and mobile pages.
- [x] 5.2 Add `tour.npcs.steps.<step>.title|content` keys for every step to the tour namespace in en/de/es/fr with real translations, and verify the tour translation test covers the new keys in every locale.

## 6. Verification and gates

- [x] 6.1 Start the full stack through the workspace Aspire AppHost (after the companion API change is applied), wait for `web` and `api` healthy, and manually verify at ≥768px and at <768px: `/library/npcs` redirects to the first NPC; `/library/npcs/makhotep?variation=necroBossWardenLHE&level=3` restores all three; Necrons faction filter + `Gauss` damage filter narrows list and variations; `blackNpc4HavocSurv` level options show health; `deathNpcMoWCrawler` and `LootObj_AmmoBox` are absent; the tour runs on both viewports; light and dark themes render the stat cards and portraits legibly. Required data state: synced game catalog only (public page, no signed-in user needed).
- [x] 6.2 Run `pnpm lint:fsd` and verify no page → page or entity → feature/page imports were introduced by `entities/npc` or `pages/library/ui/npcs`.
- [x] 6.3 Run the repository gates and verify all pass: `pnpm test:run`, `pnpm typecheck`, `pnpm lint`, `pnpm lint:fsd`, `git diff --check`.

## 7. Abilities (name + icon)

- [x] 7.1 Generate `apps/web/public/locales/{en,de,es,fr}/abilities.json` (ability id → localized name) from V1 `src/fsd/5-shared/l10n/data/<locale>/ability_names.json`, omitting ids the game leaves unnamed, register `abilities: Record<string, string>` in `shared/config/i18n/i18next.d.ts`, and verify an `ability-translations.test.ts` asserts all four locales share the key set, have no empty values, and name 303 of the 307 ability ids used by `kind: unit` NPCs (the 4 unnamed ones are internal markers, omitted by design).
- [x] 7.2 Add `abilityIcon(id)` to `packages/game-catalog/src/game-entities/icons/ability.ts` (`…/abilities/ui_icon_ability2_<id>.png`), export it from the package, and verify `icons/ability.test.ts` resolves `AdaptiveStrategy` and that the helper is a pure id→path derivation.
- [x] 7.3 Copy the 8 NPC ability icons that exist in V1 but not V2 (`TitanhammerSquad`, `IconOfObstinacy`, `TotalObliteration`, `CrucibleOfBattle`, `MyWillBeDoneOverlord`, `MultiMelta`, `HeavyWarpFlamer`, `PropheticSentinel`) into `apps/web/public/game_catalog/abilities/`, and verify the asset audit test asserts every NPC ability id either ships an icon or is one of the 6 known-absent ids.
- [x] 7.4 Build `pages/library/ui/npcs/npc-abilities.tsx` (active and passive columns side by side, stacked when `compact`, kind labelled once per column, each chip = `EntityIcon` + localized name, column omitted when empty, whole section omitted when both are empty, unnamed ids skipped), render it in `npc-detail.tsx` between attacks and traits, add the `npcs.abilities.active|passive` i18n keys in all four locales, and verify `npc-abilities.test.tsx` covers both groups, a single-group variation, the no-abilities case, and an ability with no icon rendering name-only.
- [x] 7.5 Extend `npcs-page.test.tsx` to assert the selected variation's abilities render and do not change when the level changes, and verify the full gates still pass (`pnpm test:run`, `pnpm typecheck`, `pnpm lint`, `pnpm lint:fsd`).

## 8. Attack chips and expandable ability text

- [x] 8.1 Replace `npc-attack-rows.tsx` with `npc-attacks.tsx` rendering one chip per weapon on a wrapping line (range painted on the ranged attack icon, `text-foreground` so it is legible in both themes, plus screen-reader text), and verify the page tests still assert the melee-only and melee+ranged cases and the range label.
- [x] 8.2 Generate `apps/web/public/locales/{en,de,es,fr}/npcAbilityText.json` from V1 `4-entities/abilities/data/new-ability-data.json`, including only abilities whose description resolves completely from their own variable AND constant tables (302 of 306; the 4 exceptions are the unnamed internal markers the UI drops) and trimming unreferenced entries; register the namespace type and verify `npc-ability-text-translations.test.ts` asserts equal key sets, zero unresolvable placeholders, real per-locale text, and no unused variables.
- [x] 8.3 Add `entities/npc/lib/use-npc-ability-text.ts` (mirroring `useRaidBossText`, language then `en` fallback), export it from the entity, and consume it in `npc-abilities.tsx` so an ability with resolvable text becomes an expand control rendering `shared/ability-text` at the selected level's `abilityLevel`; verify `npc-abilities.test.tsx` covers expanding, the level-scaled values, and a non-resolvable ability staying a plain chip.
- [x] 8.4 Port V1’s `NpcService.resolveRarityFromStars` as `entities/npc` `rarityFromStars` (star bands to Common..Mythic) and pass it to `AbilityText`, since 74 of the 94 entries have rarity-scaled variables and NPC rows carry no rarity; verify `rarity-from-stars.test.ts` covers every band boundary.
- [x] 8.5 Verify in the browser at 1280px and 390px that attacks read as chips, that expanding an ability shows real numbers which change with the Level selector, and that no raw `{[token]}` appears on any NPC; then re-run the gates.
- [x] 8.6 Unify the detail's horizontal rhythm: put the header selectors, attack chips and ability columns on one 2-column grid with a common gap so their column origins match, stretch chips to fill their column, right-align hit counts, and keep traits a wrapping tag row; verify in the browser that the blocks share identical column origins at 1280px and collapse to one column at 390px.
- [x] 8.7 Generate `apps/web/public/locales/en/npcTraitText.json` from V1 `4-entities/traits` (`new-traits-data.json` descriptions plus the `TRAIT_GLOBAL_VARIABLES` / `TRAIT_VARIABLE_MAP` tables parsed from its TS source), en-only with fallback as `raidBossTraitText` is; add `useNpcTraitText`, register the namespace, and open each trait's description in a popover anchored to its chip so the wrapping tag row never reflows; verify `npc-traits.test.tsx` covers the popover, a trait with no rules text, and the empty state.
- [x] 8.8 Move the desktop filters from a full-width bar into the sticky left column above the tile list (matching Character Lookup), give the filter bar an explicit `stacked` single-column variant used by both the sidebar and the mobile sheet, and scroll only the tiles so the filters stay visible; verify at 1440px that the filters and the selected tile are both on screen for a deep selection.
- [x] 8.9 Rework the filters: lift the collapsible panel into `npcs-filter-panel.tsx` shared by desktop and mobile, keep name search always visible and outside the active count, add alliance / attack-type / game-mode filters, show each option's game icon, give each multi-select its own clear that replaces the chevron, drop blank option ids, and render an explicit no-attacks state for units served without a damage profile; verify with page tests and in the browser.
- [x] 8.10 Remove the game-mode filter again before shipping: the modes are inferred from id suffixes and nothing in the served data names them, so the buckets are unconfirmed — drop `mode` from `NpcFilters`, its predicate, its option list, its control and the `npcs.mode` / `npcs.anyMode` locale keys, and file [apps#156](https://github.com/TacticusPlanner/tacticus-planner-apps/issues/156) to restore it once the taxonomy is confirmed. The `variation-mode` registry stays, since the Variation selector still labels by mode.
