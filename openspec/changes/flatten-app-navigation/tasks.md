## 1. Shared section entry-path state (desktop only)

- [x] 1.1 Add a small hook (e.g. `use-section-entry-path.ts`) in `apps/web/src/fsd/app/layout/` that, given `navItems` and the current `pathname`, tracks a `Record<sectionPath, lastVisitedChildPath>` for sections whose `children.length > 1`, initialized from and persisted to `sessionStorage` (e.g. key `nav.lastVisitedChild`, JSON-encoded, read/write wrapped in try/catch falling back to an empty record on failure), updated via a `useEffect` on `pathname` (so a visit via any surface — desktop sidebar, mobile drawer, bottom bar, or search — is recorded), and exposes a `getEntryPath(item: NavItem): string` that returns the last-visited child path if recorded and still one of the item's current `children`, else the item's own `path`
- [x] 1.2 Mount this hook once in `ShellContent` (`app-shell.tsx`), above both `DesktopShell` and `MobileShell`, so tracking survives a mid-session breakpoint switch; pass `getEntryPath` down through `shellProps`, consumed by `DesktopShell` only — `MobileShell` keeps linking every item to its own literal `path`
- [x] 1.3 Add unit tests for the hook: first visit to a multi-child section falls back to the item's own path, visiting a child route directly (not via the sidebar) still records it as last-visited, re-entering after visiting a different section returns the previously recorded child, single-child/no-children items always return their own path unchanged, a value persisted to `sessionStorage` is honored on a fresh mount (simulating reload), a stored path that's no longer one of the section's current `children` falls back to the default child, and a `sessionStorage` read/write failure falls back to the in-memory default instead of throwing

## 2. Desktop sidebar flattening

- [x] 2.1 In `desktop-layout.tsx`'s `NavMenuItem`, remove the `SidebarMenuSub`/children-rendering block entirely; the sidebar link's `to` becomes `getEntryPath(item)` instead of `item.path`, and `isActive`/`aria-current` continue to be computed from the real current `pathname` against the section's top-level `path` prefix (unchanged)
- [x] 2.2 Update `desktop-layout.test.tsx`: remove assertions expecting rendered child links/chevrons in the sidebar, add coverage that a multi-child section's sidebar link navigates to its default child on first visit and to its last-visited child after visiting one (using task 1's hook)

## 3. Nav item descriptions

- [x] 3.1 Add a `descriptionKey` field to `NavItem` and `NavSubItem` in `nav-items.ts` (typed the same way as `labelKey`), and populate it for every existing top-level item (Home, Lookup, Goals, Dailies, Progress, Guild, UI Kit) and every existing child item (Lookup's Character/MoW/NPC, Goals' Projects/Insights, Progress' Onslaught/Campaigns/Campaign Events/XP Income, Guild's Members)
- [x] 3.2 Add the corresponding description string for every `descriptionKey` to every supported locale (`de`, `en`, `es`, `fr`), following the sibling-leaf naming convention from design.md (e.g. `nav.homeDescription`, `unitLookup.tabs.characterDescription`)
- [x] 3.3 Update `desktop-navigation-dialog.tsx` to render each result's description beneath its label, for both top-level and child results
- [x] 3.4 Update `mobile-layout.tsx`'s drawer rendering and `mobile-drawer-sub-item.tsx` to render each item's description beneath its label, for both the top-level entry and its nested children
- [x] 3.5 Extend `navigation-filter.ts`'s `filterNavigationItems` to match a query against an item's description as well as its label (both top-level and child items), so the shared function keeps serving both the desktop dialog and the mobile drawer's search field
- [x] 3.6 Add/update tests: description renders for a top-level and a child item in both the desktop dialog and the mobile drawer; a query matching only a description (not the label) still returns that item in both surfaces

## 4. Page header description

- [x] 4.1 In `app-shell.tsx`, alongside the existing `pageTitle` derivation, compute `pageDescription`: find the active top-level item as today, then check whether the current pathname matches (or is nested under) one of its `children`; if so use that child's `descriptionKey`, otherwise use the active top-level item's own `descriptionKey`; pass `pageDescription` down through `shellProps`
- [x] 4.2 In `desktop-layout.tsx`'s header, wrap the existing `<h1>` title in a flex-column container and render `pageDescription` beneath it as a truncated `text-sm text-muted-foreground` line
- [x] 4.3 In `mobile-layout.tsx`'s `MobileHeader`, render `pageDescription` beneath the title as a truncated secondary line, and in `mobile-layout.css` increase `--mobile-header-height` by enough to fit the added line (see design.md — every mobile consumer of this variable resizes automatically, no other code change needed)
- [x] 4.4 Add tests: the header shows the active top-level section's description on that section's own page (including Home and Dailies, which have no matching child item); the header swaps to a child's own description when a child page is active; the description updates when navigating between sibling child pages
- [x] 4.5 Update `desktop-layout.test.tsx`/`mobile-layout.test.tsx` snapshots or assertions that check the header's rendered content, if any, to account for the added description line

## 5. Desktop keyboard shortcuts

- [x] 5.1 Add a small shared helper (e.g. `is-mac-platform.ts` in `apps/web/src/fsd/app/layout/`) that checks `navigator.userAgent`/`navigator.platform` for macOS, used by every shortcut hint below instead of duplicating the check
- [x] 5.2 In `desktop-layout.tsx`'s `AppSidebar`, add a `window`-level `keydown` listener (mirroring `SIDEBAR_KEYBOARD_SHORTCUT`'s exact pattern in `packages/ui/src/components/sidebar.tsx`) that, on `(event.metaKey || event.ctrlKey) && event.key === "k"`, calls `event.preventDefault()` and toggles the existing `navigationOpen` state; clean up the listener on unmount
- [x] 5.3 Add a platform-appropriate shortcut hint ("⌘K" on macOS, "Ctrl+K" elsewhere) next to the Search button's label in `AppSidebar`, using `CommandShortcut` from `packages/ui/src/components/command.tsx`
- [x] 5.4 Extend the same `AppSidebar` listener from 5.2 to also handle `(event.metaKey || event.ctrlKey) && event.key === "g"`, calling `event.preventDefault()` and invoking the existing `onCreateGoal` prop
- [x] 5.5 Add a platform-appropriate shortcut hint ("⌘G" on macOS, "Ctrl+G" elsewhere) next to the Create Goal button's label in `AppSidebar`
- [x] 5.6 In `player-data-sync-button.tsx`, add its own `window`-level `keydown` listener that, on `(event.metaKey || event.ctrlKey) && event.shiftKey && event.key === "s"` (or `"S"`, matching however `Shift` affects `event.key` — verify against the existing sidebar-shortcut pattern's key-matching approach), calls `event.preventDefault()` and invokes `syncNow()` only when `!isSyncing`; clean up the listener on unmount
- [x] 5.7 Add a platform-appropriate shortcut hint ("⌘⇧S" on macOS, "Ctrl+Shift+S" elsewhere) next to the Sync button's label in `player-data-sync-button.tsx`
- [x] 5.8 Add tests: pressing Ctrl/Cmd+K opens `DesktopNavigationDialog` when closed, closes it when already open, fires regardless of which element has focus, and the sidebar's Search button renders the shortcut hint; pressing Ctrl/Cmd+G calls `onCreateGoal` and the Create Goal button renders its hint; pressing Ctrl/Cmd+Shift+S calls `syncNow` when not already syncing, is a no-op while `isSyncing` is true, and the Sync button renders its hint

## 6. Regression coverage

- [x] 6.1 Confirm `mobile-layout.tsx`'s drawer structure and `mobile-drawer-sub-item.tsx` require no structural changes beyond the description rendering from task 3.4 — the nested child-listing behavior itself is explicitly unchanged by this capability; update `mobile-layout.test.tsx` only for the new description assertions, not for any structural removal
- [x] 6.2 Add/confirm a test on `desktop-navigation-dialog.tsx` and the mobile drawer's search field that searching a child page's label still returns and links directly to that child page's route
- [x] 6.3 Run `general.tutorial.test.tsx` and confirm it still passes unmodified (steps target only top-level `desktop-nav-*`/`mobile-nav-*` testids per design.md Context, not sidebar/drawer child items) — no tutorial code changes expected; only fix if this assumption turns out wrong
- [x] 6.4 Verify `scroll-to-top-button.tsx` and `character-lookup-mobile-page.tsx` (the other consumers of `--mobile-header-height`) still position correctly against the taller mobile header from task 4.3
- [x] 6.5 Confirm the new Ctrl/Cmd+K, Ctrl/Cmd+G, and Ctrl/Cmd+Shift+S listeners don't conflict with each other, with the existing Ctrl/Cmd+B sidebar-toggle shortcut, or with any browser/OS-reserved shortcut for the same key combinations in supported browsers (see design.md's key-choice rationale)

## 7. Verification

- [x] 7.1 Run `pnpm test:run`, `pnpm typecheck`, `pnpm lint`, and `pnpm lint:fsd` for `tacticus-planner-apps`
- [ ] 7.2 Manually verify in the running app at one viewport below 768px and one at or above 768px, while authenticated (so every multi-child section is visible): the desktop sidebar shows only top-level sections with no chevrons or nested children, while the mobile menu drawer still shows each section's child pages nested beneath it exactly as before; clicking a multi-child section (Lookup, Progress, Goals) in the desktop sidebar for the first time in a session lands on its default child; visiting a non-default child (via that section's own tab bar, the mobile drawer, or search), switching to another top-level section, then re-clicking the original section in the desktop sidebar returns to the previously visited child; reloading the page after visiting a non-default child still returns to that child on the next desktop sidebar click (sessionStorage persistence); Guild (single-child) still navigates to its existing default route every time on desktop; Dailies returns to its last-visited child on desktop, the same as other multi-child sections, not its default route on every entry
- [ ] 7.3 Manually verify desktop and mobile navigation search still finds and opens a child page (e.g. "Machines of War") directly, and that both the mobile drawer and desktop search dialog show a short description beneath every item's label, for both top-level sections and child pages, in at least two supported locales
- [ ] 7.4 Manually verify the page header on both desktop and mobile: shows the section's description on a section's own page, swaps to the active child's description on a child page, and updates correctly when switching between sibling child pages
- [ ] 7.5 Manually verify, on a desktop-width viewport (and on macOS if available, in addition to Windows/Linux): Ctrl/Cmd+K opens and closes the navigation search dialog from anywhere; Ctrl/Cmd+G opens the Create Goal sheet from anywhere; Ctrl/Cmd+Shift+S starts a sync from anywhere and is a no-op while a sync is already in progress; all three sidebar buttons show the correct shortcut hint for the current platform
- [ ] 7.6 Manually run both the desktop and mobile general/navigation Joyride tour to confirm no step is broken by the sidebar flattening or the taller mobile header
- [x] 7.7 Run `git diff --check` and OpenSpec strict validation before wrapping up

## 8. Consolidate page-level tab bars into the shared header (revision)

Product review of the shipped result (groups 1-7) found the new header description sitting above an _unchanged_ page-level tab bar duplicated title+description up to three times per page (header, tab bar's page, and that page's own pre-existing title+description — see design.md's Context and Decisions for the full survey). This group moves every section's tab bar into the shared header, including Dailies, superseding groups 3/4's "Dailies keeps its section-level description" and "title stays fixed to the section" decisions.

- [x] 8.1 Add `children` to Dailies in `nav-items.ts` (Raids, Shops, Onslaught, Salvage Run, Arena, Guild Raids, in that order, matching `dailies-layout.tsx`'s existing tab order and the route config's `/dailies/raids` index-redirect default), each with a `labelKey`/`descriptionKey` under a new `dailies.tabs.*` key group in `common.json`; extend the `NavLabelKey` union accordingly (the `NavDescriptionKey` template type covers the description keys automatically)
- [x] 8.2 Add the new `dailies.tabs.*` label + description strings to `common.json` for every supported locale (`de`, `en`, `es`, `fr`) — labels sourced from the existing `dailies` namespace's `tabs.*` strings, the Raids description sourced from `raids.description` (see 8.4), the other five from short new copy (they have no existing per-page description to reuse — see design.md Context)
- [x] 8.3 Replace the placeholder `descriptionKey` text written in group 3 for Goals > Projects/Insights and Progress's four tabs with each page's real existing copy, across all four locales: `goals.tabs.projectsDescription` ← `goals.project.pageDescription`, `progress.tabs.onslaughtDescription` ← `onslaught.description`, `progress.tabs.campaignsDescription` ← `progress.campaigns.description`, `progress.tabs.campaign-eventsDescription` ← `progress.events.description`, `progress.tabs.xp-incomeDescription` ← `progress.xpIncome.description`; Goals > Insights had no existing description (only a title) — keep a short written one
- [x] 8.4 Add a new shared header-tabs component (e.g. `apps/web/src/fsd/app/layout/section-tabs.tsx`) that renders a routed `Tabs`/`TabsList`/`TabsTrigger` row from a `NavItem`'s `children` (label from each child's `labelKey`, active tab from the current pathname, `onValueChange` navigates), reusable by both desktop and mobile headers
- [x] 8.5 In `app-shell.tsx`, change `pageTitle` to resolve to the active child's own `labelKey` when `resolveActiveNavigation` finds one (mirroring how `pageDescription` already resolves), falling back to the active top-level item's own `labelKey` otherwise; pass the active top-level item's `children` (if any) down through `shellProps` for the header-tabs component
- [x] 8.6 Wire the header-tabs component into `desktop-layout.tsx`'s header, directly beneath the title/description; remove `goals-layout.tsx`'s, `progress-layout.tsx`'s, `apps/web/src/fsd/pages/lookup/ui/lookup-page.tsx`'s, and `guild-registered-view.tsx`'s own tab bar implementations; remove `dailies-layout.tsx`'s top-level `RouteTabs` call (its nested `RaidsLayout`'s own Today/Plan `RouteTabs` call is unaffected — third level, stays in page content)
- [x] 8.7 Wire the header-tabs component into `mobile-layout.tsx`'s header the same way, beneath title/description; increase `--mobile-header-height` again if needed to fit the added tab row (verify against the same consumers as task 4.3/6.4: `scroll-to-top-button.tsx`, `character-lookup-mobile-page.tsx`)
- [x] 8.8 Delete the now-redundant title+description block from `apps/web/src/fsd/pages/goals/ui/projects/projects-page.tsx`, `.../insights/insights-page.tsx`, `apps/web/src/fsd/pages/onslaught/ui/onslaught-page.tsx`, `apps/web/src/fsd/pages/progress/ui/campaigns-page.tsx`, `.../campaign-events-page.tsx`, `.../xp-income-placeholder-page.tsx`, and `apps/web/src/fsd/pages/dailies/ui/raids-layout.tsx`; for each removed i18n key (`goals.project.title`/`pageDescription`, `goals.insights.title`, `onslaught.title`/`description`, `progress.campaigns.title`/`description`, `progress.events.title`/`description`, `progress.xpIncome.title`/`description`, `raids.title`/`description`), grep for other usages (tests, other components) before deleting it from every locale — keep the key if still referenced elsewhere, else delete it there too
- [x] 8.9 Update tests for the moved tab bars and the removed per-page blocks: `desktop-layout.test.tsx`/`mobile-layout.test.tsx` gain coverage that the header renders the active section's tab row and that clicking a header tab navigates correctly (including Dailies' six); `goals-layout.test.tsx` and any `progress-layout`/`lookup-page`/`guild-registered-view`/`dailies-layout` tests lose their own tab-bar assertions; `projects-page.test.tsx`, `insights-page.test.tsx`, Progress's four page tests, and `raids-layout.test.tsx` (if present) lose their title/description assertions
- [x] 8.10 Update/add coverage for Dailies now being a multi-child section: desktop sidebar last-visited-child behavior (per the new spec scenario "Dailies follows the same multi-child behavior as any other section"), and the mobile drawer now showing Dailies' six tabs nested beneath it
- [x] 8.11 Re-run `pnpm test:run`, `pnpm typecheck`, `pnpm lint`, `pnpm lint:fsd`, `git diff --check`, and `openspec validate --strict`; redo the manual verification in tasks 7.2-7.6 covering the new header-tabs behavior across all five sections (Lookup, Goals, Progress, Guild, Dailies), including that no page renders a duplicate title/description/tab row anymore (automated portion done; manual browser verification still pending, per earlier explicit user choice to verify manually)

## 9. Desktop header: static section title with an inline/breadcrumb child-picker (revision)

Product review of the shipped group-8 result found the desktop header's stacked two-row layout (title/description row, then a full-width tab row beneath it) undesired — the requested layout puts the section's own children inline in the _same_ row as the title (e.g. `Lookup   Character  Machines of War  NPC`), with the title itself staying pinned to the section name rather than swapping to the active child (that swap now applies to `pageDescription` and to mobile's title only, not desktop's title). A referenced source mockup additionally showed a breadcrumb-dropdown pattern ("Dailies › Raids ⌄") for a section with too many children to list inline; this group also builds that, scoped to a fixed child-count threshold rather than real responsive width measurement — see design.md's "Desktop header composition" decision.

- [x] 9.1 Update `proposal.md` and `specs/app-navigation/spec.md` to document: desktop header title stays pinned to the top-level section (never swaps to the active child); a new inline child-picker sits beside it (tab row for 4 or fewer children, breadcrumb dropdown for more, i.e. Dailies); `pageDescription` is unaffected and keeps swapping to the active child's own description on both platforms; mobile's header (title, description, and tab row) is unchanged
- [x] 9.2 In `app-shell.tsx`, compute a new `sectionTitle` value (`activeItem ? t(activeItem.labelKey) : undefined` — the top-level item's own label, never a child's), alongside the existing child-swapping `pageTitle`; pass both through `shellProps` — `pageTitle` consumed by `MobileShell` only (unchanged), `sectionTitle` by `DesktopShell` only
- [x] 9.3 Add a new component `apps/web/src/fsd/app/layout/desktop-section-header.tsx` (`DesktopSectionHeader`) that, given the active top-level item and `sectionTitle`: renders a plain `<h1>` for a childless section; renders the `<h1>` plus a compact inline `Tabs`/`TabsList`/`TabsTrigger` row for a section with `children.length <= 4`; renders a `DropdownMenu` trigger reading "`{Section} › {Active child} ⌄`" (replacing the plain title) for a section with `children.length > 4` (today, only Dailies), opening a menu of every child that navigates on selection
- [x] 9.4 Wire `DesktopSectionHeader` into `desktop-layout.tsx`'s `DesktopShell` header in place of the previous `<h1>` + `SectionTabs` stacked layout; `pageDescription` keeps rendering as its own row beneath, unchanged; remove the now-unused `SectionTabs` import from `desktop-layout.tsx` (mobile's `mobile-header.tsx` keeps using `SectionTabs` unchanged)
- [x] 9.5 Update `desktop-layout.test.tsx`: rename every `pageTitle` prop passed directly to `DesktopShell`/`EntryPathHarness` to `sectionTitle`; add a test proving the header title stays the section name ("Lookup") on a child route (`/lookup/mow`) while the description still swaps to the child's own description; add a test for Dailies (6 children) rendering the breadcrumb dropdown instead of a tab row, showing the active child in its label, and navigating correctly when a different child is selected from the menu
- [x] 9.6 Re-run `pnpm test:run`, `pnpm typecheck`, `pnpm lint`, `pnpm lint:fsd`, `git diff --check`, and `openspec validate --strict`; manual browser verification of the new desktop header layout (inline tabs for Lookup/Goals/Progress/Guild, breadcrumb dropdown for Dailies) remains part of the still-pending manual verification in tasks 7.2/7.4, per earlier explicit user choice to verify manually

## 10. Desktop header follow-up: plain nav-row links, wider inline threshold, and a scrollbar fix

Product feedback on the group-9 result: don't use the `Tabs` component for the desktop header's inline child-picker — stack the children as plain nav items in one row instead (matching the breadcrumb dropdown's own minimal menu-item look). Also requested previewing Dailies (6 children) inline instead of behind the dropdown. Separately, reviewing a screenshot of Dailies > Raids' own nested Today/Plan sub-tabs surfaced a pre-existing, unrelated CSS bug: a spurious vertical scrollbar on any horizontal tab row styled with `overflow-x-auto` alone.

- [x] 10.1 In `desktop-section-header.tsx`, replace the inline branch's `Tabs`/`TabsList`/`TabsTrigger` usage with a plain `<nav>` of `react-router` `Link` elements (one per child), styled as minimal text nav items (`text-foreground` active / `text-muted-foreground hover:text-foreground` inactive, `aria-current="page"` on the active one) instead of a boxed/pill tab control; `useNavigate`/`onValueChange` is no longer used in this branch
- [x] 10.2 Raise `MAX_INLINE_TABS` from 4 to 6 (today's largest section, Dailies) so every current section renders inline; the breadcrumb-dropdown branch and its code stay in place for a future section that grows past 6, just unreached by any current data
- [x] 10.3 Fix the spurious-vertical-scrollbar bug by adding `overflow-y-hidden` alongside `overflow-x-auto` on `section-tabs.tsx` (mobile header tabs) and `dailies-layout.tsx`'s `RouteTabs` (used by `raids-layout.tsx`'s Today/Plan sub-tabs) — root cause: per the CSS Overflow spec, setting only `overflow-x: auto` forces `overflow-y` to also compute as `auto` instead of staying `visible`, so any sub-pixel vertical overflow (e.g. the "line" tab variant's underline) gets clipped-and-scrolled
- [x] 10.4 Update `desktop-layout.test.tsx`: scope the "no child links in the sidebar" assertion to the sidebar's own DOM subtree (child links now legitimately exist in the header too) via `within(...)`; replace the Dailies breadcrumb-dropdown test with one asserting all six children render inline; add a new `desktop-section-header.test.tsx` unit-testing `DesktopSectionHeader` directly, including a synthetic 7-child fixture to keep the now-unreached-by-real-data dropdown branch covered; re-run `pnpm test:run`, `pnpm typecheck`, `pnpm lint`, `pnpm lint:fsd`, `git diff --check`, and `openspec validate --strict`
